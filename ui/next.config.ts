import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // React 19 has built-in compiler optimizations
  // No need for explicit reactCompiler config in Next.js 16 with React 19
};

export default nextConfig;
